# Data Base Configuration (MariaDB)
DB_HOST=localhost
DB_USER=mariadb_user
DB_PASSWORD=mariadb_password
DB_NAME=fungivora_db or fungivora_dummy
# Backend Port Configuration
BACKEND_PORT=0000
# Allowed Hosts for Vite Development Server (comma-separated)
ALLOWED_HOSTS=another-host.com